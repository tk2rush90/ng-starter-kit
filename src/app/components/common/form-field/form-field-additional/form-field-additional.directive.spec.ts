import { FormFieldAdditionalDirective } from './form-field-additional.directive';

describe('FormFieldAdditionalDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFieldAdditionalDirective();
    expect(directive).toBeTruthy();
  });
});
