import { SelectionChangeDetectorDirective } from './selection-change-detector.directive';

describe('SelectionChangeDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionChangeDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
