import { GoogleOauthHandlerDirective } from './google-oauth-handler.directive';

describe('GoogleOauthHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleOauthHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
