import { IntersectionDetectorDirective } from './intersection-detector.directive';

describe('IntersectionDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectionDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
