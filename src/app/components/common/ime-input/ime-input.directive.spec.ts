import { ImeInputDirective } from './ime-input.directive';

describe('ImeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ImeInputDirective();
    expect(directive).toBeTruthy();
  });
});
