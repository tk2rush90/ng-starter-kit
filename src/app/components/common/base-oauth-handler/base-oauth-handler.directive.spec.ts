import { BaseOauthHandlerDirective } from './base-oauth-handler.directive';

describe('BaseOauthHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseOauthHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
