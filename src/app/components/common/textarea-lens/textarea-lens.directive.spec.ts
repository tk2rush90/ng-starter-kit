import { TextareaLensDirective } from './textarea-lens.directive';

describe('TextareaLensDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaLensDirective();
    expect(directive).toBeTruthy();
  });
});
