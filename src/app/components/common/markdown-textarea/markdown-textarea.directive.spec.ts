import { MarkdownTextareaDirective } from './markdown-textarea.directive';

describe('MarkdownTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkdownTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
