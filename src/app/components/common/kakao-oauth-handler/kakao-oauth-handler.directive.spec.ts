import { KakaoOauthHandlerDirective } from './kakao-oauth-handler.directive';

describe('KakaoOauthHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new KakaoOauthHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
